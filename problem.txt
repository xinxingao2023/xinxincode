关闭随机基质后

把壳代码(stub.dll)的重定位表拷贝到被加壳程序的新节区

之后因为加壳代码的text段被拷贝到新的节区，改变其重定位表里面的VirtualAddress

改后VirtualAddress=之前VirtualAddress-textRVA+新节区的RVA

之后再改变数据目录里面重定位表里面的RVA指向拷贝过来壳代码的重定位表的RVA

如果成功壳代码就可以运行起来

但是不行

用PE工具看改变了的重定位表是正确的
